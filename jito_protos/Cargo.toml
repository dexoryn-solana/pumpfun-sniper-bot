[package]
name = "jito-protos"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.13.5"
prost-types = "0.13.5"
tonic = { version = "0.12.3", features = ["tls", "tls-roots", "tls-webpki-roots"] }

[build-dependencies]
tonic-build = "0.12.3"

